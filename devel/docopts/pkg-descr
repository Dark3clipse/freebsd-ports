reflink_dedupe is a tool to deduplicate files using
reflinks on supported filesystems. It provides a
command-line interface to identify duplicate files,
replace them with reflinks, and optionally update
associated configuration and log files.
